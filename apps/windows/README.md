# Windows App

The Windows app should be native .NET, likely WinUI 3, and should consume shared PhonoDeck contracts and fixtures instead of sharing Apple UI code.

Do not implement Windows by embedding the React web app as the primary shell unless a later architecture decision explicitly changes this.
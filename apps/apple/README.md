# Apple Apps

Apple platform apps use Swift and SwiftUI.

Planned targets:

- macOS: primary app and current implementation.
- iOS: companion/mobile app using shared Swift core.
- watchOS: remote/session companion.
- tvOS: living-room playback/control surface where provider policy allows it.

Current source remains in `Sources/PhonoDeck` until the Swift core split is complete.